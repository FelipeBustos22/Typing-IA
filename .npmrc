# Bloquear ejecución de scripts de paquetes terceros
ignore-scripts=true

# Guardar versiones exactas por defecto
save-exact=true

# Nivel mínimo de audit para fallar
audit-level=high

# Forzar uso de lockfile
package-lock=true

# Registro público (explícito)
registry=https://registry.npmjs.org/
